version '1.0-SNAPSHOT'

dependencies {
    implementation project(':plat-sys:c-plat-sys')
}

ext {
    snippetsDir = file('build/generated-snippets')
}

test {
    outputs.dir snippetsDir
}

asciidoctor {
    inputs.dir snippetsDir
    dependsOn test
}