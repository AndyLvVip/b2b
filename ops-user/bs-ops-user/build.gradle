version '1.0-SNAPSHOT'

dependencies {
    implementation project(':ops-user:c-ops-user')
}

ext {
    snippetsDir = file('build/generated-snippets')
}

test {
    outputs.dir snippetsDir
}

asciidoctor {
    inputs.dir snippetsDir
    dependsOn test
}