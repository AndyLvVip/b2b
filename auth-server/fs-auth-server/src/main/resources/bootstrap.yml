spring:
  application:
    name: fs-auth-server
  cloud:
    alibaba:
      seata:
        tx-service-group: b2b_tx_group

  profiles:
    active: default

---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://192.168.63.195:8888