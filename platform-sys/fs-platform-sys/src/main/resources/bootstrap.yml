spring:
  application:
    name: fs-platform-sys
  profiles:
    active: default
  cloud:
    alibaba:
      seata:
        tx-service-group: b2b_fs_tx_group
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      default-transaction-isolation: 2
management:
  endpoints:
    web:
      exposure:
        include: "*"
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://192.168.63.195:8888